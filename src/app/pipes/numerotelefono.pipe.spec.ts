import { NumerotelefonoPipe } from './numerotelefono.pipe';

describe('NumerotelefonoPipe', () => {
  it('create an instance', () => {
    const pipe = new NumerotelefonoPipe();
    expect(pipe).toBeTruthy();
  });
});
